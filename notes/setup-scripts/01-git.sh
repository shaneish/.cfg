# git
# dnf install git

git config --global user.email "stephenson.shane.a@gmail.com"
git config --global user.name "Shane Stephenson"
git config --global credential.helper store

# git clone https://github.com/oatish/cfg.git $CFG_PATH
